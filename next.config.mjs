/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  // server: {
  //   host: '0.0.0.0', // เพื่อให้รับการเชื่อมต่อจากทุก IP
  //   port: 3000,
  // },
};

export default nextConfig;
